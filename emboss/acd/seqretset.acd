# SEQRET application
# AJAX COMMAND DEFINITION (ACD) FILE

# use "" for missing values - these are required.
# values in "" are trimmed to single spaces.
# everything is treated as single tokens delimited by white space
# (space, tab, newline)

# pmr 22-apr-98

appl: seqretset [
	doc: "Reads and writes (returns) a set of sequences all at once"
	groups: "reformatting, sequence display"
]

  seqset: sequence [ param: Y ]

  seqoutset: outseq [ param: Y ]
